# Dependances Python du projet (poste de travail / notebook).
#
# NB : les DAGs Airflow s'executent DANS le conteneur apache/airflow:2.10.4,
# qui embarque deja boto3 et requests — rien a installer pour eux.
# Ce fichier sert au notebook d'exploration (Data/data.ipynb) et aux
# scripts locaux.
#
# Installation :
#   python -m venv .venv
#   .venv\Scripts\Activate.ps1        (Windows)
#   pip install -r requirements.txt

# Exploration / preparation des donnees (Data/data.ipynb)
pandas>=2.0
soccerdata>=1.8
jupyter>=1.0

# Acces S3 (SeaweedFS) et APIs depuis des scripts locaux
boto3>=1.34
requests>=2.31

# Application NL->SQL (nl2sql-app)
sqlglot>=25        # validation applicative des requetes generees (parsing AST)
streamlit>=1.40    # interface du banc d'essai (app.py)
